# Vultr deployment guide written + production build verified

- **Date:** 2026-05-19 14:00 CEST
- **Author:** ai-session-01
- **Role:** ops
- **Phase (from TODO.md):** 6 (submission prep)

## What changed

- Added `DEPLOY.md` — opinionated, step-by-step Vultr + Coolify guide. 10 steps, ~40 min first-time deploy.
- Added a link to it in `README.md`'s doc index.
- Verified `pnpm build` succeeds locally (Next.js 16.2.6, 2 static routes prerendered, no errors).

## Why

Hackathon requires a Vultr VM deployment + public demo URL for Best-of-Vultr eligibility. Picking Coolify over PM2/Caddy because: auto-TLS, GitHub auto-deploy, and it's mentioned in the Vultr partner workshop list — judges may recognize the stack.

## Files touched

- `DEPLOY.md` (new)
- `README.md` (doc index entry)

## How to verify

Local build still works:

```bash
pnpm build
```

Should print `✓ Compiled successfully` and list `/` + `/_not-found` routes.

## Next steps / handoff

For **BOT** (whoever owns ops):

- [ ] Provision Vultr VM (Step 1 in `DEPLOY.md`) — Frankfurt or Amsterdam, 2 vCPU / 4 GB RAM.
- [ ] Pick a domain — `nip.io` for hackathon speed (Step 2).
- [ ] Run the Coolify installer on the VM (Step 3).
- [ ] Wire the GitHub repo into Coolify (Step 5), paste env vars from `.env`.
- [ ] First deploy (Step 6), bind domain (Step 5 sub-step), verify TLS.
- [ ] Apply `supabase/schema.sql` if BE hasn't already (Step 7).
- [ ] Set the Telegram webhook to `https://<domain>/api/telegram/webhook` (Step 8).
- [ ] Turn on auto-deploy from `main` (Step 9).
- [ ] Smoke-test the public URL (Step 10).
- [ ] Post the public URL in the team chat.

For **AI** (me): nothing remaining for deployment.

## Blockers

- The `TELEGRAM_BOT_TOKEN` env var won't be set until BOT creates the bot via @BotFather. That blocks Step 8 only — Steps 1–7 and 9–10 work without it.

## Cost note

Vultr 2vCPU/4GB instance is ~$24/mo. Hackathon usage = ~$1–2 from the $200 credit pool.

## Recommendation

Start Steps 1–4 (VM + Coolify install) **now** while AI/BE/FE are still iterating. Coolify install takes 10 minutes of waiting that can run in the background. Then connect the repo and deploy whatever's on `main` even before final code lands — every push will auto-redeploy.
