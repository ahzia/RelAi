# Backend Phase 3: onboarding + agent create

- **Date:** 2026-05-19 16:00 CEST
- **Role:** be
- **Phase:** 3 (BE)

## What changed

- `POST /api/onboarding` — parse Telegram answers, `extractProfileSafe`, upsert `attendees`
- `POST /api/agents/create` — create `agents` row, return `agentId` + `dashboard_url`
- `lib/onboarding/parse-answers.ts`, `extract-profile-safe.ts` (Gemini + `USE_DEMO_FALLBACK` stub)
- DB: `upsertAttendeeFromProfile`, `createAgentForAttendee`, `getAttendeeByTelegramChatId`
- `pnpm check:onboarding` smoke test

## Verify

```bash
pnpm check:onboarding
```

Example curl (stub profile if `USE_DEMO_FALLBACK=true`):

```bash
curl -X POST http://localhost:3000/api/onboarding -H "Content-Type: application/json" -d "{\"telegram_chat_id\":12345,\"answers\":[\"Alex\",\"Founder at RelAI\",\"AI, events\",\"Investors\",\"Tuesday PM\"]}"
```

## Handoff BOT

On onboarding complete: `POST /api/onboarding` → `POST /api/agents/create` → send `dashboard_url` to user.
