# Initial scaffold + docs + Supabase wiring

- **Date:** 2026-05-19 13:00 CEST
- **Author:** ai-session-01
- **Role:** setup
- **Phase (from TODO.md):** 0 (most), 1 (Supabase schema)

## What changed

- Created the planning docs: `ARCHITECTURE.md`, `TODO.md`, `README.md`, `CONTRIBUTING.md`, `LICENSE` (MIT), `.gitignore`, `.env.example`. Trimmed `hackathon_info.md` from 2257 → 185 lines.
- Scaffolded **Next.js 16.2.6 + TypeScript + Tailwind v4 + ESLint** into the repo (`pnpm` via corepack). Package name is `relai` (lowercase, npm requires it).
- Installed project deps: `grammy`, `@supabase/supabase-js`, `@google/generative-ai`, `reactflow`, `zod`, `tsx`.
- Approved pnpm 11 build-script gate for `sharp`, `unrs-resolver`, `esbuild` via `pnpm-workspace.yaml`.
- Created the directory skeleton from `ARCHITECTURE.md §4` (`app/api/...`, `lib/{db,gemini,telegram,orchestrator,seed}`, `components/{graph,feed,matches,ui}`, `supabase/`, `scripts/`).
- Added npm scripts: `dev`, `build`, `start`, `lint`, `typecheck`, `db:seed`, `bot:dev`.
- Switched env vars to Supabase's new key model: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, `SUPABASE_DATABASE_PASSWORD`, `SUPABASE_SECRET_KEY`. Updated `ARCHITECTURE.md §11`.
- Wrote `lib/db/clients.ts` with `getBrowserClient()` (publishable key, RLS-gated) and `getServerClient()` (secret key, RLS-bypass).
- Wrote `scripts/check-supabase.ts` smoke test — both keys verified against the live project (`https://akivfzefaqseiahusspw.supabase.co`).
- Wrote `supabase/schema.sql` with all 5 tables, indexes, and idempotent RLS policies (public SELECT for the publishable key).

## Why

Unblock 4 parallel workstreams (FE / BE / AI / BOT) so the team can start without bikeshedding stack or env layout. Hackathon submission is **today 16:00 CEST**.

## Files touched

- `README.md`, `ARCHITECTURE.md`, `TODO.md`, `CONTRIBUTING.md`, `LICENSE`, `.gitignore`, `.env.example`, `hackathon_info.md`
- `package.json`, `pnpm-workspace.yaml`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `postcss.config.mjs`
- `app/page.tsx`, `app/layout.tsx`, `app/globals.css` (Next.js defaults — FE should replace)
- `lib/db/clients.ts`
- `scripts/check-supabase.ts`
- `supabase/schema.sql`

## How to verify

```bash
pnpm install
pnpm typecheck                                           # passes
pnpm dev                                                 # http://localhost:3000 returns 200
pnpm exec tsx --env-file=.env scripts/check-supabase.ts  # both keys connect
```

## Next steps / handoff

- [ ] **Anyone:** apply `supabase/schema.sql` via Dashboard → SQL Editor (or `supabase db push`). Verify with the SQL: `select table_name from information_schema.tables where table_schema = 'public';` — should list 5 tables.
- [ ] **BE:** write `scripts/seed-attendees.ts` (30–50 fake profiles from `Idea.md §Fake Data Strategy`) and implement the read-only API routes from `ARCHITECTURE.md §6`.
- [ ] **FE:** replace `app/page.tsx` with the RelAI landing page and start the dashboard shell at `app/dashboard/[agentId]/page.tsx`.
- [ ] **AI:** stub `lib/gemini/client.ts` and the 4 prompt files; start with canned JSON outputs (see `Idea.md §Gemini Prompts`).
- [ ] **BOT:** finish Vultr VM provisioning + Coolify, deploy a "hello world" build first, then iterate.
- [ ] Fill in owner names in `TODO.md` and `README.md`.

## Blockers

None. All four tracks can start in parallel.
