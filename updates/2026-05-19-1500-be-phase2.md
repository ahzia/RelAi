# Backend Phase 2: demo workflow + POST start

- **Date:** 2026-05-19 15:00 CEST
- **Author:** cursor-agent
- **Role:** be
- **Phase (from TODO.md):** 2

## What changed

- `lib/orchestrator/persist-event.ts` — maps `WorkflowEvent` → `graph_events` rows (reused in Phase 4).
- `lib/seed/demo-fallback.ts` — `runDemoFallback(agentId)` with paced events (~25–30s).
- `POST /api/agents/:id/start` — returns 202 immediately, runs demo in background.
- DB helpers: `updateAgentStatus`, `getAgentStatus`, `deleteGraphEventsForAgent`, `deleteMatchesForAgent`.
- `pnpm demo:start` — CLI to run workflow without HTTP.
- `check:backend` now smoke-tests POST start.

## How to verify

```bash
pnpm db:seed
pnpm dev
# In another terminal:
pnpm demo:start
# Or:
curl -X POST http://localhost:3000/api/agents/b0000000-0000-4000-8000-000000000001/start
```

Open the dashboard and watch status + graph activity update while the script runs.

## Next steps

- [ ] Phase 4: replace `runDemoFallback` with `runAgentWorkflow` + DB persistence in `lib/orchestrator/run.ts`.
- [ ] FE: poll `/graph` every 1.5s on the dashboard.
