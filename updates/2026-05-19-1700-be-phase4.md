# Backend Phase 4: real orchestrator wired to POST start

- **Date:** 2026-05-19 17:00 CEST
- **Role:** be
- **Phase:** 4

## What changed

- `lib/orchestrator/run.ts` — `runAgentWorkflowFromDb` + `runAgentNetworking`
  - Loads agent persona + attendee pool (excludes self + demo users)
  - Clears prior `graph_events` / `matches`, runs `runAgentWorkflow`
  - `onEvent` → `graph_events` + `agents.status` updates
  - Saves top 3 `should_meet` matches + conversations
- `POST /api/agents/:id/start` now calls `runAgentNetworking`
- `USE_DEMO_FALLBACK=true` → scripted demo (no Gemini)
- Gemini failure → automatic fallback to `runDemoFallback`

## Verify

```bash
# Scripted (fast, no API cost):
USE_DEMO_FALLBACK=true pnpm demo:start

# Real Gemini (~30–60s):
USE_DEMO_FALLBACK=false pnpm demo:start
# Or POST /api/agents/<agentId>/start with dev server running
```

## Env

```env
USE_DEMO_FALLBACK=false   # use Gemini
GEMINI_API_KEY=...        # required when false
```
